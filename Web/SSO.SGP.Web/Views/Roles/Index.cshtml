@using Syncrosoft.Framework.Controles.MVC.Script;
@using Syncrosoft.Framework.Controles.Mvc.JQuery.Datatables;
@using SSO.SGP.Web.Controllers;
@using Syncrosoft.Framework.Controles.MVC;
@using SSO.SGP.EntidadesDeNegocio.Vistas;
@model SSO.SGP.EntidadesDeNegocio.webpages_Roles

<!--
    CODE GENERATED BY Xeus Technology
    Date:  17/07/2016 17:48:47
    Version: 3
-->
@{
    Layout = null;
    var hash = "form" + Session.GetHashCode();
    var value = "";
}
<!-- SCRIPTS -->
<!-- <script src="ts/Roles/Index.js"></script> -->
<!-- /SCRIPTS -->
@Scripts.Render("~/bundles/jqueryval")
@using (Html.BeginForm("Index", "Roles", FormMethod.Post, new { id = hash }))
{
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                @{
                    ColumnCommand[] operadoresRoles = new ColumnCommand[2] {

                 new ColumnCommand {
                    display = "editar",
                    function = "oRoles.editar_dtRoles(this)",
                    icon = "../assets/img/icons/16x16/edit.png",
                    tooltip = "Editar este registro"
                 },

                 new ColumnCommand {
                    display = "eliminar",
                    function = "oRoles.eliminar_dtRoles(this)",
                    icon = "../assets/img/icons/16x16/delete.png",
                    tooltip = "Eliminar este registro"
                 },
                                 };
                    var vmRoles = Html.DataTableVm("dtRoles", (RolesController h) => h.getRolesGrid(null));
                    vmRoles.operators = operadoresRoles;
                    vmRoles.OpcionesEnGrilla = true;
                    //vmRoles.fnDrawCallBack = "render_dtRoles(this);";
                    vmRoles.ColumnFilter = true;
                    vmRoles.OrdenarPor = new Dictionary<int, string>();
                    vmRoles.OrdenarPor.Add(1, "asc");
                    //vmRoles.FilterOn("activo").Select("Si", "No");
                    vmRoles.CantidadRegistrosDefault = 25;
                }
                @Html.Partial("~/Views/Shared/DataTable.cshtml", vmRoles)
                <input type="hidden" id="SelectedRoles@(hash)" value="" />
            </div>

        </div>
        <div class="form-actions">
        </div><!--fin form-actions>-->
    </div>
                    }
<script type="text/javascript">
    $(function () {
        app.Desbloquear();
    });
    var viewIndex = new Roles.ts.Index("@(hash)");
    oRoles.setIndex(viewIndex);
    viewIndex.setRoles(dtRoles);
</script>