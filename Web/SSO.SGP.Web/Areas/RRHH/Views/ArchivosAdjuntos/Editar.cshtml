@using Syncrosoft.Framework.Controles.MVC.Script;
@using Syncrosoft.Framework.Controles.Mvc.JQuery.Datatables;
@using Xeus.Sentencia.Web.Controllers;
@using Syncrosoft.Framework.Controles.MVC;
@using Xeus.Sentencia.EntidadesDeNegocio.Vistas;
@model Xeus.Sentencia.EntidadesDeNegocio.ArchivosAdjuntos

<!--
    CODE GENERATED BY Xeus Technology
    Date:  28/07/2016 22:02:35
    Version: 3    
-->
@{
    Layout = null;
    var hash = "form" + Session.GetHashCode();
	var value = "";
}
<!-- SCRIPTS -->
<!-- <script src="ts/ArchivosAdjuntos/Editar.js"></script> -->
<!-- /SCRIPTS -->

@using (Html.BeginForm("Editar", "ArchivosAdjuntos", FormMethod.Post, new { id = hash }))
{
    <div class="container-fluid">
								<div class="row">
				<div class="span12 m-wrap">
					<div class="control-group">
	<label class="control-label" for="Path">Path</label>
	<div class="controls">		
		<input type="text" class="form-control col-md-12" id="Path@(hash)" name="Path" tabindex="2" value="@((Model!=null ) ? ((Model.GetType().GetProperty("Path") != null) ? Model.Path.ToString() : "") : "")" />
	</div>
</div>	
			</div>			
								</div>
			<!--fin de row-->
							<div class="row">
				<div class="span12 m-wrap">
					<div class="control-group">
	<label class="control-label" for="Nombre">Nombre</label>
	<div class="controls">		
		<input type="text" class="form-control col-md-12" id="Nombre@(hash)" name="Nombre" tabindex="3" value="@((Model!=null ) ? ((Model.GetType().GetProperty("Nombre") != null) ? Model.Nombre.ToString() : "") : "")" />
	</div>
</div>	
			</div>			
								</div>
			<!--fin de row-->
							<div class="row">
				<div class="span12 m-wrap">
					<div class="control-group">
	<label class="control-label" for="Descripcion">Descripcion</label>
	<div class="controls">		
		<input type="text" class="form-control col-md-12" id="Descripcion@(hash)" name="Descripcion" tabindex="4" value="@((Model!=null ) ? ((Model.GetType().GetProperty("Descripcion") != null) ? Model.Descripcion.ToString() : "") : "")" />
	</div>
</div>	
			</div>			
								</div>
			<!--fin de row-->
							<div class="row">
				<div class="span12 m-wrap">
					<div class="control-group">
	<label class="control-label" for="Sentencia">Sentencia</label>
	<div class="controls">
				<select id="Sentencia@(hash)" class="form-control  col-md-12" name="Sentencia" tabindex="1">	
		@foreach (var e in (IEnumerable<SelectOptionsView>)ViewBag.Sentencia) {	
			if ((Model != null) && (Model.GetType().GetProperty("Sentencia") != null)) {
				if (Model.Sentencia.ToString() == e.value.ToString()) { 
					<option selected value="@e.value">@e.text</option>
				} else {
					<option value="@e.value">@e.text</option>
				}
			}
			else {
				<option value="@e.value">@e.text</option>
			}
		}
		</select> 
		<script type="text/javascript">
			$(function () {
				$('#Sentencia@(hash)').chosen();
			});
		</script>  
	</div>
</div>
			</div>			
								</div>
			<!--fin de row-->
							<div class="row">
				<div class="span12 m-wrap">
					<div class="control-group">
	<label class="control-label" for="Sumario">Sumario</label>
	<div class="controls">
				<select id="Sumario@(hash)" class="form-control  col-md-12" name="Sumario" tabindex="1">	
		@foreach (var e in (IEnumerable<SelectOptionsView>)ViewBag.Sumario) {	
			if ((Model != null) && (Model.GetType().GetProperty("Sumario") != null)) {
				if (Model.Sumario.ToString() == e.value.ToString()) { 
					<option selected value="@e.value">@e.text</option>
				} else {
					<option value="@e.value">@e.text</option>
				}
			}
			else {
				<option value="@e.value">@e.text</option>
			}
		}
		</select> 
		<script type="text/javascript">
			$(function () {
				$('#Sumario@(hash)').chosen();
			});
		</script>  
	</div>
</div>
			</div>			
				
	</div>
		<div class="form-actions">
			
		<input type="button" value="Cancelar" id="Cancelar@(hash)" name="Cancelar" class="btn red" />
			
		<input type="button" value="Guardar" id="Guardar@(hash)" name="Guardar" class="btn green" />
			</div><!--fin form-actions>-->
	</div>
}
<script type="text/javascript">
    $(function () {
        app.Desbloquear();
    });
	var viewEditar = new ArchivosAdjuntos.ts.Editar("@(hash)");
    oArchivosAdjuntos.setEditar(viewEditar);
	</script>